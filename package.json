{
  "name": "signatur-generator",
  "private": true,
  "version": "1.1.45",
  "description": "Lokale Webapp zum Erstellen von E-Mail-Signaturen",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/setup-githooks.mjs",
    "bump:patch": "node scripts/bump-version.mjs",
    "verify": "node --check app.js && node --check templates.js && node --check version.js && node --check analytics-consent.js && node --check pwa.js && node --check service-worker.js && node scripts/check-version.mjs"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
